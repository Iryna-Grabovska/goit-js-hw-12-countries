{{#each this}}
<article>
  <h1>{{name}}</h1>
  <p>Capital:{{capital}} </p>
  <p>Population:{{population}} </p>
  <p>Languages:{{laguges}} </p>
  <img src="{{flag}}" alt="{{name}}">
</article>
{{/each}}